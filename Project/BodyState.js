// Need to complete the package of java script
// need to import the functions for calling them do not for get that


export class BodyState{

	// Need to look at the final key word properly
		constructor(state)
		{
			this.state = state;
		} 
}

	FED_RESTING(0);
	FED_EXERCISING(1);
	FED_POSTEXERCISE(2);
	POSTABSORPTIVE_RESTING(3);
	POSTABSORPTIVE_EXERCISING(4); 
	POSTABSORPTIVE_POSTEXERCISE(5);

BodyState.NUM_STATES = 6;